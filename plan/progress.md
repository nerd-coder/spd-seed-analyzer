# v4 close-out progress

Index: [README.md](README.md). Facts: `specs/analysis/`. Decks: `specs/generator-decks.md`.

## Done

- **00–07 closed.** 07 is a deny-list; 00–06 shipped without porting it.
- Imp vault Playwright map (`AAA-AAA-AAA` F19 B1). `471331d`.
- AAA SacrificeRoom prize is v4 Java **Sai** (cell 1141 Crossbow is TrapsRoom). `f4b2365`.
- Alchemy / well-water blobs `alwaysVisible`. `b5e7971`.

## Now

Nothing queued from the close-out plan.

## Next

Leftover lib reds with no Java fixture: analyze_smoke seeds, shop `public.builder` redaction, search Ghost/sickle. Analyzer still `partial`.

## Remaining

Those smoke/search/shop tests. Analyzer still `partial`.
